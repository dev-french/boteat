const MealPlan = require('../models/MealPlan');

const mealPlanController = {
    createMealPlan: async (req, res) => {
        // Logique pour créer un nouveau plan de repas
    }
};

module.exports = mealPlanController;
